#!/usr/bin/env bash
set -euo pipefail
ROOT="$(cd "$(dirname "$0")" && pwd)"
if [[ -x "$ROOT/.venv/bin/python" ]]; then
  exec "$ROOT/.venv/bin/python" "$ROOT/restore_bak_to_csv.py" "$@"
fi
exec python3 "$ROOT/restore_bak_to_csv.py" "$@"
